pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building project...'
                // For Windows use: bat 'dir'
                bat 'dir'
            }
        }
    }
}
